# Core RAG pipeline + CPU fallback OCR.
# For the real Baidu Unlimited-OCR model, also install requirements-gpu.txt on a CUDA machine.
langchain>=0.2
# Provides RetrievalQA on langchain>=1.0 (harmless on older versions).
langchain-classic>=1.0; python_version >= "3.10"
langchain-community>=0.2
langchain-openai>=0.1
langchain-text-splitters>=0.2
faiss-cpu>=1.7
sentence-transformers>=2.6
pymupdf>=1.24
pillow>=10.0
python-dotenv>=1.0
tqdm>=4.66

# CPU fallback OCR (pip-only, no system binary needed)
rapidocr-onnxruntime>=1.3

# Web chat UI
streamlit>=1.36
